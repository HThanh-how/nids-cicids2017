% =====================================================================
%  Network Intrusion Detection on CICIDS2017
%  Springer CCIS format (LNCS class)  --  target venue: SOICT 2026
%
%  GOLDEN RULE: every number in this paper comes from
%  experiments/results_v3/*.json via make_tables.py and make_figures.py.
%  Nothing measured is typed by hand.
% =====================================================================
% SOICT requires the submitted PDF to carry no page numbers, so the
% running heads are dropped for submission.
\documentclass{llncs}
% SOICT 2026 is single-blind: \anonfalse keeps author information visible.
% Set \anontrue only for a double-blind venue.
\newif\ifanon\anonfalse

% T1 encoding: the default OT1 font has no underscore glyph, so a DOI such
% as 10.1007/978-3-031-31108-6_2 printed without its "_" in the references.
\usepackage[T1]{fontenc}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{amsmath}
\usepackage{multirow}
\usepackage{array}
\usepackage{url}
\usepackage{tikz}
\usetikzlibrary{positioning,fit,arrows.meta}
% NOTE: hyperref intentionally NOT loaded — it clashes with llncs
% (\@xdblarg error). Springer adds hyperlinking for the final version.

% Tables are generated by experiments/make_tables.py directly from
% experiments/results_v3/*.json, so no measured number is typed by hand.
% A stage that has not been run yet simply contributes no table.
\newcommand{\inputtable}[1]{%
  \IfFileExists{tables/#1.tex}{\input{tables/#1}}{}}

% Figures are generated by experiments/make_figures.py from the same
% JSON as the tables. A figure whose stage has not run is skipped rather
% than breaking the build.
\newcommand{\resfig}[3]{%
  \IfFileExists{figures/#1.pdf}{%
    \begin{figure}[htbp]\centering
    \includegraphics[width=0.88\textwidth]{figures/#1}
    \caption{#2}\label{#3}
    \end{figure}}{}}

% The paper carries many small floats; the default separation costs more
% than a page across the document.
\setlength{\textfloatsep}{9pt plus 2pt minus 2pt}
\setlength{\intextsep}{9pt plus 2pt minus 2pt}
\setlength{\floatsep}{7pt plus 2pt minus 2pt}
\renewcommand{\topfraction}{0.9}
\renewcommand{\bottomfraction}{0.7}
\renewcommand{\textfraction}{0.08}
\renewcommand{\floatpagefraction}{0.8}

% The reference list is the last thing on the page budget; removing the
% inter-item glue keeps it on the final body page without touching any
% entry.
\let\oldthebibliography\thebibliography
\renewcommand{\thebibliography}[1]{%
  \oldthebibliography{#1}%
  \setlength{\itemsep}{0pt}\setlength{\parsep}{0pt}%
  \setlength{\parskip}{0pt}}

\pagestyle{empty}
\begin{document}
\thispagestyle{empty}

\title{How Optimistic Is Your Intrusion-Detection Benchmark?
Leakage-Resistant Evaluation and Measured Serving Cost}

\ifanon
\author{Anonymous Author(s)}
\institute{Affiliation withheld for double-blind review}
\else
% Two authors, one institution. The email domain matches the affiliation;
% to switch the corresponding address, change only the \email line.
\author{Huy-Thanh Pham \and Tuan-Anh Truong}
\authorrunning{H.-T. Pham and T.-A. Truong}
\institute{Faculty of Computer Science and Engineering,\\
Ho Chi Minh City University of Technology (HCMUT), VNU-HCM,
Ho Chi Minh City, Vietnam\\
\email{thanh.phamhuy@hcmut.edu.vn, anhtt@hcmut.edu.vn}}
\fi

\maketitle

\begin{abstract}
Machine-learned intrusion detectors report near-perfect scores on public
benchmarks, and practitioners do not see that performance in the field. We
show that a large part of the discrepancy is produced by the evaluation
itself, and we measure how large. Holding the models, the hyperparameters and
the preprocessing fixed on CICIDS2017, and re-fitting feature selection,
oversampling and scaling inside every training split, we compare a
conventional stratified random split against a day-disjoint split in which no
attack family is shared between training and test. Attack recall for XGBoost
falls from 0.9935 to 0.3164 and macro-F1 from 0.9879 to 0.6887; Random Forest
behaves the same way. The ranking of models does not survive either: Logistic
Regression, the weakest detector under the random split, becomes the
strongest under the day-disjoint one, so a benchmark that flatters a model can
also select the wrong one. Repeating the conventional protocol over five
seeds further shows that the usual claim of a best model is unsupported --
XGBoost leads Random Forest by 0.00044 macro-F1, which neither a paired
$t$-test nor a Wilcoxon signed-rank test separates from zero. We then
translate the measured error rates into precision at operational attack
prevalence, where benchmark precision does not transfer, and we measure the model served as a
containerised endpoint under controlled load: it sustains 404~requests/s
before latency collapses, where the amortised per-sample model cost -- the
figure the literature, and an earlier version of this work, mistakes for a
service throughput -- would predict $9.5\times10^{5}$~flows/s, an
overstatement by a factor of roughly 2{,}300. The experiment
driver, the serving implementation, the deployment manifests and the raw
result files from which every table and figure here is generated are released
with the paper.
\keywords{Intrusion detection \and Evaluation methodology \and Data leakage
\and CICIDS2017 \and Model serving \and Network security.}
\end{abstract}

% =====================================================================
\section{Introduction}
\label{sec:intro}
Learning-based intrusion detection reports macro-F1 above 0.98 as a matter
of routine on public benchmarks, and practitioners do not observe it in the
field. The discrepancy is usually attributed to concept drift or to the age
of the corpora. We examine a more immediate contributor: much of the reported
accuracy is an artefact of how the benchmark is split and of what the metric
measures.

A random split rewards recognition rather than detection: flows from one
attack episode are numerous and near-identical, a random split scatters them
across the train/test boundary, and a model scores well by recognising
near-duplicates it has memorised. That this flatters intrusion detectors has
been observed before~\cite{moczkodan2026transformers,xu2025robust}; we do not
claim the observation but contribute a protocol that removes the confound
and a measurement of what it costs. Separately, precision depends on
prevalence, and benchmarks carry orders of magnitude more attack traffic
than a production link. And papers routinely support a deployment claim with
an \emph{amortised per-sample model cost}, which omits parsing,
serialisation, transport and concurrency and is not a property of a service;
an earlier version of this work converted such a figure into a throughput,
which prompted this study.

\paragraph{Contributions.}
Holding models, hyperparameters and preprocessing fixed and varying only the
evaluation, we contribute: a pipeline in which scaling, selection and
oversampling are re-fitted \emph{inside} every training split
(Section~\ref{sec:leakage}); four splitting protocols on identical data --
repeated holdout, day-disjoint (here also family-disjoint),
leave-one-family-out and cross-dataset transfer (Section~\ref{sec:method});
paired tests over repeated seeds showing the usual ranking of the leading
models to be unsupported (Section~\ref{sec:paired}); error rates translated
into precision and alert burden at operational prevalence
(Section~\ref{sec:operational}); and an end-to-end measurement of the
deployed service, including replication and failure recovery, reported
separately from model cost (Section~\ref{sec:serving}). The driver, service,
container and Kubernetes definitions, load profiles and raw results -- from
which every table and figure is generated -- are released with the
paper.\footnote{\url{https://github.com/HThanh-how/nids-cicids2017}}

\section{Related Work}
\label{sec:related}
Buczak and Guven~\cite{buczak2016survey} survey the method families; our
concern is evaluation rather than models.

NSL-KDD~\cite{tavallaee2009nslkdd} corrected redundant records in KDD'99,
UNSW-NB15~\cite{moustafa2015unswnb15} introduced a contemporary feature set,
and CICIDS2017~\cite{sharafaldin2018cicids} added five days of labelled
traffic across several attack families. CICIDS2017 is not a neutral
instrument: Engelen et al.~\cite{engelen2021troubleshooting} document
labelling and capture defects -- misordered and duplicated packets, executed
but unlabelled attacks -- and Lanvin et al.~\cite{lanvin2023errors} show that
correcting them changes measured performance materially. We therefore report
differences between protocols on identical data rather than absolute scores.

That the evaluation of learned intrusion detectors is the weak link is an
old observation. Axelsson~\cite{axelsson1999baserate} showed that at
realistic attack prevalence the false-positive rate, not the detection rate,
bounds usefulness; Sommer and Paxson~\cite{sommer2010closedworld} argued
that closed-world benchmarks systematically overstate what machine learning
delivers on live traffic; Arp et al.~\cite{arp2023lessons} catalogue the
recurring pitfalls -- sampling bias, data snooping, base-rate neglect -- and
Kapoor and Narayanan~\cite{kapoor2023leakage} document leakage as a general
cause of irreproducible machine-learning results. That random splits flatter
intrusion detectors in particular is likewise not new, and we do not claim
it. Moczkodan and Ragab~\cite{moczkodan2026transformers} compare random,
temporal and five-tuple-grouped splits on this corpus and find the random
convention substantially overestimates performance; Xu and
Liu~\cite{xu2025robust} report severe recall loss on novel attacks. We extend
the observation along three axes they leave open: a day-disjoint split, which
here is simultaneously family-disjoint; a translation of error rates into
precision at operational prevalence; and a measurement of serving cost. That
last is treated most loosely in the literature, where an amortised per-sample
model cost -- typically of order $10^{-2}$~ms -- is offered as evidence of
deployability although it excludes parsing, serialisation, transport,
queueing and concurrency. Table~\ref{tab:related} summarises the positioning;
recent applied studies~\cite{talukder2024ml,almuhanna2025dl} confirm the
benchmark's continued use.

\inputtable{related_work}

\section{Data and Preprocessing}
\label{sec:data}
CICIDS2017~\cite{sharafaldin2018cicids} holds five working days of labelled
flows -- benign traffic plus denial-of-service, DDoS, brute-force,
web-attack, botnet, port-scan and infiltration families -- each described by
more than eighty flow statistics, in eight CSV files, one per capture period.
We draw a fixed 20\,\% stratified sample from each. After normalising column
names, replacing infinities, dropping incomplete records and removing exact
duplicates, the working set holds 535{,}069 flows with 78 numeric features
and an attack ratio of 0.1769.

Each flow keeps its \emph{capture day}, recovered from its source file, and
its \emph{original multi-class label}: the first makes the day-disjoint split
possible, the second the per-family analysis. The binary target maps every
non-benign label to the positive class. Standardisation, top-$k$ selection by
Random Forest importance~\cite{breiman2001random} and oversampling with
SMOTE~\cite{chawla2002smote} are treated as part of the model, not of the
data (Section~\ref{sec:leakage}).

\section{Evaluation Protocol}
\label{sec:method}
The models in this study are deliberately ordinary. What varies, and what the
paper is about, is the protocol under which they are evaluated. This section
fixes that protocol precisely enough to be re-run.

\subsection{Models, Hyperparameters and Leakage Control}
\label{sec:leakage}
We compare Logistic Regression, Random Forest~\cite{breiman2001random},
XGBoost~\cite{chen2016xgboost} and a two-hidden-layer perceptron, with every
hyperparameter fixed a priori and identical across every split, seed and
dataset; we perform no tuning, so no model is advantaged by a larger search
budget and nothing can have been selected on test data. Logistic Regression:
$\ell_2$, $C=1$, \texttt{lbfgs}, 1000 iterations. Random Forest: 200 trees,
unlimited depth, $\sqrt{d}$ features per split, Gini. XGBoost: 300 rounds,
depth 8, learning rate 0.3, no subsampling, $\lambda=1$, \texttt{hist}.
Perceptron: layers $(128,64)$, ReLU, Adam, $\alpha=10^{-4}$, batch 200,
learning rate $10^{-3}$, 50 epochs with early stopping. The machine-readable
specification is released with the artefact.

Preprocessing fitted before the split leaks test information into training,
and feature selection is the worst offender. For every split in every
protocol, therefore, the scaler is fitted on the training portion only, the
Random-Forest ranking selecting the top $k=20$ features is computed on the
scaled training portion only, and SMOTE~\cite{chawla2002smote} is applied to
the training portion only, after selection. The selected set is thus a random
variable; its stability is reported in Section~\ref{sec:featstab}.

\subsection{Four Splitting Protocols}
The same pipeline is evaluated under four separations of training from test;
only the last two resemble what a deployed sensor faces.
\textbf{(P1) Repeated stratified holdout} -- a 70/30 split over five seeds,
the conventional protocol and our reference point.
\textbf{(P2) Day-disjoint} -- training on Monday--Wednesday, testing on
Thursday--Friday, which removes the near-duplicate flows a random split
scatters across the boundary. Each attack family on CICIDS2017 was executed
on one day, so this split is also \emph{family-disjoint}: every attack at
test time is of a type absent from training.
\textbf{(P3) Leave-one-family-out} -- one family removed from training
entirely and the model asked to flag it, separating the family boundary from
the day boundary.
\textbf{(P4) Cross-dataset transfer} -- fitted on one corpus, evaluated on
the other with no target-domain data, on an explicit feature alignment
(Section~\ref{sec:transfer}). We distinguish this from
\emph{independent-dataset replication}, re-running the protocol within
UNSW-NB15; the two are frequently conflated, including in an earlier version
of this work, and only P4 speaks to cross-domain behaviour.

\subsection{Statistical Treatment and Metrics}
\label{sec:stats}
Every repeatable protocol is run over multiple seeds with all models on
identical splits, so comparisons are paired; we report means with 95\,\%
confidence half-widths, and for the leading pair a paired $t$-test and a
Wilcoxon signed-rank test. Alongside accuracy, precision, recall and
macro-F1 we report false-positive and false-negative rates, MCC, balanced
accuracy, ROC-AUC and PR-AUC, the last being the informative one under heavy
imbalance. Precision depends on prevalence while TPR and FPR do not, so a
benchmark precision does not transfer to deployment; for prevalence $\pi$,
\begin{equation}
\label{eq:ppv}
\mathrm{PPV}(\pi)=\frac{\pi\cdot\mathrm{TPR}}
{\pi\cdot\mathrm{TPR}+(1-\pi)\cdot\mathrm{FPR}},
\end{equation}
evaluated at $\pi \in \{10^{-1},\dots,10^{-4}\}$ with the implied false
alerts per million flows.


% =====================================================================
\section{Results}
\label{sec:results}
We report the four protocols of Section~\ref{sec:method} in order, then the
feature-selection analysis and the operational reading of the error rates.
Every table and figure below is generated directly from the stored
experiment output, and every number quoted in the text is cross-checked
against the same files by a script released with the artefact.

% [trimmed for page limit]
%\inputtable{dataset}
% [trimmed for page limit] the two day groups are named in the text
%\inputtable{days}
\subsection{P1 and P2: Random versus Day-Disjoint}
\label{sec:p1}
Table~\ref{tab:protocols} reports both protocols on identical data. Under the
random split the two tree ensembles lead clearly, and the interval
half-widths deserve as much attention as the means: they are tight for Random
Forest and XGBoost and an order of magnitude wider for Logistic Regression
and the MLP, whose macro-F1 ranges over 0.908--0.978 across seeds, so a
single-split number for either is close to uninformative.

\inputtable{protocols}
% [trimmed for page limit] \resfig{fig_seed_spread}

\subsection{Is the Leading Model Actually Leading?}
\label{sec:paired}
The usual conclusion from a table like this is that XGBoost wins.
A paired test over the shared seeds says otherwise. XGBoost does lead Random Forest, but by 0.00044 macro-F1, and the paired
$t$-test does not separate that from zero ($p=0.13$); the Wilcoxon
signed-rank test agrees ($p=0.19$), though with five seeds its smallest
attainable two-sided $p$ is 0.0625, so the effect size carries the argument:
the difference is an order of magnitude below the seed-to-seed spread of the
weaker models. Both ensembles are separated from Logistic Regression and the
MLP by margins that are large and clearly significant.

We therefore do not name a best model. What the data supports is two tiers --
tree ensembles above the linear and shallow neural baselines -- with the
choice inside the upper tier resting on other grounds, which
Section~\ref{sec:cost} shows are not negligible.

% [trimmed for page limit] the two comparisons that matter are
% quoted in the text
%\inputtable{paired_tests}

\subsection{P2: Day-Disjoint, and the Optimism of a Random Split}
\label{sec:p2}
Training uses Monday--Wednesday and testing Thursday--Friday; training holds
the denial-of-service variants, both brute-force families and Heartbleed,
testing holds botnet, DDoS, infiltration, port scanning and the three
web-attack variants. Attack recall for XGBoost falls from 0.9935 to 0.3164
and for Random Forest from 0.9937 to 0.3109, macro-F1 by roughly 0.30 for
both: models that find essentially every attack under the conventional
protocol miss two in three under this one (Figure~\ref{fig:gap}).
Figure~\ref{fig:confusion} shows where the errors go. The false-positive rate \emph{improves}, from 0.0073 to
0.0030, while false negatives rise from 185 flows to 36{,}506: the detector
does not become noisy, it becomes quiet, confident and wrong -- the failure
a dashboard is least likely to reveal.

Nor does the ranking survive. Logistic Regression is last under the random
split (macro-F1 0.8515 against 0.9879) and first under the day-disjoint one
(0.7433 against 0.6887), with attack recall 0.4975 against 0.3164. The gap is
largest for the models that score best, so the conventional protocol can
recommend the wrong model; Section~\ref{sec:operational} qualifies what that
inversion does and does not imply. The day boundary removes near-duplicate
flows and attack families at once; the next section separates them.

\resfig{fig_optimism_gap}{The same pipeline, hyperparameters and data
evaluated two ways. Filled markers are the random split, hollow markers the
day-disjoint split; the bar between them is the optimism of the conventional
protocol. The ordering is not preserved.}{fig:gap}
\resfig{fig_confusion}{Row-normalised confusion for XGBoost, mean over seeds,
raw counts in parentheses. The false-positive rate improves under the
day-disjoint protocol while false negatives rise two orders of magnitude.}
{fig:confusion}

\subsection{P3: Per-Family Behaviour and Unseen Attack Types}
\label{sec:p3}
Aggregate scores average over families weighted by flow count.
Figure~\ref{fig:family} breaks the random-split result down: a macro-F1 of
0.988 coexists with XGBoost detecting SQL injection at 0.000 and cross-site
scripting at 0.023 -- families of a few hundred flows that cost the aggregate
nothing and are not the ones a defender would choose to miss. And the model
that looks worst in aggregate is the only one that detects them: Logistic
Regression reaches 1.00 on XSS and 0.93 on web brute force where the three
stronger models sit between 0.02 and 0.09, by way of a false-positive rate an
order of magnitude higher. Whether that is a virtue is a question about the
alert budget, which Section~\ref{sec:operational} makes quantitative.

\resfig{fig_per_family}{Recall by attack family under the random split. The
model with the weakest aggregate score is the only one that detects the
web-attack families at all.}{fig:family}

Leave-one-family-out removes a family from training entirely, and the outcome
divides rather than grades. Where a sibling variant remains, generalisation
holds: holding out DoS~Hulk or DoS~slowloris still yields recall near 0.88.
Where the family stands alone it collapses: botnet traffic is detected at
0.000 and port scanning at 0.0036 across 26{,}822 held-out flows. A
supervised flow classifier of this kind interpolates within a family and does
not extrapolate to a new one; missing 99.6\,\% of port scanning is not
anomaly detection in any useful sense.

\subsection{Feature Selection: Reproducible but Not Canonical}
\label{sec:featstab}
Across seeds the top-20 selection is stable (mean pairwise Jaccard 0.756; 15
of 20 chosen by every seed). Across criteria it is not: impurity and
permutation importance, on the same split with the same forest, agree on 4
of 20 -- impurity favours packet-length statistics, permutation puts
destination port, forward inter-arrival minimum and initial TCP window sizes
first, and mutual information sides with impurity (0.75). Either list yields
a similar macro-F1, so no aggregate score reveals this; a published top-$k$
list is a property of the criterion as much as of the traffic.

\subsection{Independent Replication, and P4: Cross-Dataset Transfer}
\label{sec:transfer}
Re-running the whole protocol \emph{within} UNSW-NB15 gives XGBoost macro-F1
0.9504 and Random Forest 0.9489, so the two-tier ordering reproduces on
independent data. That is replication, not transfer: no model leaves its own
corpus.

Transfer fits on one corpus and evaluates on the other with no target-domain
data. The corpora share no schema, so we align the seven flow-level
quantities whose semantics match -- duration, forward and backward packet
and byte counts, mean packet sizes -- rescaling CICIDS2017 duration from
microseconds to seconds. The restriction is a confound, so we measure it: an
in-domain holdout on exactly these seven features gives XGBoost 0.9911 on
CICIDS2017 and 0.9289 on UNSW-NB15 against 0.9879 and 0.9504 on twenty, so
what follows is domain shift, not feature starvation.

Transfer fails in both directions, and differently in each. Fitted on
CICIDS2017 and tested on UNSW-NB15 the models call almost everything benign:
attack recall 0.0000 for Random Forest, 0.0001 for XGBoost, 0.0229 for the
MLP. Fitted on UNSW-NB15 and tested on CICIDS2017 they flag almost
everything: false-positive rates 0.55--0.89, precision 0.11--0.21 despite
recall up to 0.85. Each failure runs towards the majority decision of its
source corpus -- 17.7\,\% attack against 68.1\,\% -- so what transfers is a
threshold calibrated to one dataset's prevalence, not a notion of malicious
traffic. Macro-F1 lands between 0.24 and 0.42 against 0.99 in-domain. The
conclusion is narrow: a flow-statistics classifier trained on one public
corpus does not carry to another, which contradicts citing an in-domain score
as evidence of general applicability; domain adaptation and recalibration on
target traffic are untested here.

\subsection{What the Error Rates Mean in Operation}
\label{sec:operational}
Every number so far was measured at 17.7\,\% attack prevalence. Since TPR and
FPR do not depend on prevalence, Equation~\ref{eq:ppv} carries them to a
realistic one (Table~\ref{tab:operational}). All four models collapse, and
the collapse is governed by the false-positive rate -- Axelsson's base-rate
fallacy~\cite{axelsson1999baserate}, here with measured rates. The most favourable case
in this paper -- XGBoost, random split, recall 0.9935, FPR 0.0073 -- has
precision 0.120 at one attack in a thousand, seven of every eight alerts
false and 7{,}257 false alerts per million flows; at one in ten thousand,
0.013.

\inputtable{operational}

The comparison across protocols is easy to misread. Under the day-disjoint
protocol XGBoost's alert burden \emph{improves} -- 7{,}257 to 3{,}006 false
alerts per million, precision 0.120 to 0.095 -- while recall falls from
0.9935 to 0.3164. An operator watching alert volume would see a quieter,
no-less-accurate system; the degradation is invisible in exactly the signals
a deployment has. Nor does the inversion of Section~\ref{sec:p2} recommend
Logistic Regression: at one in a thousand its precision is 0.0083 with
59{,}143 false alerts per million, twenty times XGBoost's burden for a recall
advantage of 0.18. Neither the conventional protocol nor macro-F1 ranks
detectors once prevalence is accounted for. The UNSW-NB15 replication
sharpens this: recall above 0.96 there comes with FPR near 0.075, so
precision at one in a thousand is 0.013 rather than 0.120 -- an order of
magnitude of operational value between two standard benchmarks.

\subsection{Computational Cost}
\label{sec:cost}
Timings are the mean of five repeats after a discarded warm-up, on a 16-core
AMD workstation with 27.8~GiB running Python 3.13, scikit-learn 1.5.2,
XGBoost 3.4.1 and imbalanced-learn 0.14.2. XGBoost trains in
$5.96 \pm 1.41$~s against $32.65 \pm 2.48$~s for Random Forest, a sixth of
the cost for a quality difference Section~\ref{sec:paired} could not
distinguish from zero; cost is the only basis for the choice this paper
found. Amortised inference is 0.001056~ms per flow for XGBoost -- model cost
spread over a full batch on all cores, a lower bound on serving a flow, which
we do not convert into a throughput.

\section{Deployment and Measured Serving Cost}
\label{sec:serving}
An offline score says what a detector would decide; it does not say what the
system around it costs to run. This section closes that gap by measuring the
deployed service rather than describing it.

\subsection{The Service and the Load Protocol}
The model is exported with the standardisation statistics of its features
and wrapped in a stateless FastAPI service with liveness, readiness (failing
until the model is resident), single-flow and batch endpoints; every
response carries the in-process scoring time. The image is pinned, non-root,
one worker per replica. The artefact served is the artefact evaluated. Load
is closed-loop -- each of $N$ clients issues its next request as the previous
returns, so saturation cannot hide behind an unbounded queue -- with a
discarded warm-up, held-out CICIDS2017 flows as payloads, and the latency
distribution reported to the 99.9th percentile.

\subsection{Results}
\label{sec:servingresults}
At the library's default thread setting a single replica sustains
309~requests/s at eight clients (median 24~ms, p95 39~ms), then collapses:
188~requests/s at 32 clients with p95 578~ms, 128~requests/s at 128 clients
with p99 6.2~s, no request failing at any level. The amortised model cost
of Section~\ref{sec:cost} inverts to about $9.5\times10^{5}$~flows/s; the
best configuration we measured sustains 404~requests/s
(Section~\ref{sec:replication}), an overstatement by a factor of roughly
2{,}300. Batching recovers much but not all of it: a batch of 256 reaches
34{,}662~flows/s, still 27 times below the extrapolation, at p95 88~ms -- 114
times the flow throughput for twice the median request latency, a policy
choice invisible in any offline comparison. An earlier version of this work
quoted $10^{5}$~predictions/s on exactly this basis. Process launch to first
successful readiness takes 7.83~s, of which 1.95~s is loading and warming
the model.

\subsection{Replication, Failure and the Resource Envelope}
\label{sec:replication}
Three properties usually deferred to a cluster need only more than one
replica and the willingness to kill one. We run $N$ service processes,
round-robin the load at eight clients per replica, kill one under load, and
record CPU and memory throughout (Figure~\ref{fig:scaling}).

\resfig{fig_scaling}{Replica scaling on a single host. Whether replication
adds capacity is decided by one environment variable: with the maths
libraries free to use every core, throughput \emph{falls} as replicas are
added.}{fig:scaling}

Each request scores one 20-feature row, and by default the numerical
libraries fan that across every core, so replicas contend: throughput falls
from 269 to 88~requests/s as replicas go from one to eight. Pinning each
replica to one maths thread -- what the image sets, and what we had
previously asserted without evidence -- is worth 1.5 to 2.8 times at every
replica count, and 404 against 269~requests/s even for a single replica,
where no inter-replica contention exists. Past two replicas the pinned curve
also declines, but the load generator shares the sixteen cores, so that
portion is bounded by client contention; the thread setting is the part that
transfers to any host.

Killing one of four replicas under load, three times, lost exactly 35
requests of roughly 8{,}000 each time; the replica served again after 3.13~s
and throughput returned to baseline 8~s after the kill. A Kubernetes Service
would also deregister the dead endpoint, so 35 is an upper bound. Resident
memory is 176~MiB per replica pinned, 189--196~MiB unpinned, flat in
replica count; our Deployment had requested 512~MiB and limited at 1~GiB,
chosen before any measurement, and now derives both from it. What still
needs a cluster -- image pull, pod placement and the autoscaler's control-loop
delay -- is not measured and not estimated.

\section{Limitations}
\label{sec:limitations}
CICIDS2017 carries documented labelling and capture
defects~\cite{engelen2021troubleshooting,lanvin2023errors}, so absolute
scores on it are not field accuracy; the differences between protocols on
identical data that we emphasise are far more robust to label noise, but
their size on a corrected or modern corpus is not established. Day-disjoint
is not the only leakage-resistant split -- five-tuple or timestamp
grouping~\cite{moczkodan2026transformers} separates near-duplicates from
unseen families more cleanly. Hyperparameters are fixed a priori; the perceptron's seed-to-seed spread
may partly reflect its 50-epoch budget rather than its capacity, and we would
expect tuning against a random-split validation set to widen the gap rather
than close it.
The operational precision figures assume the measured rates transfer to a
link with a different traffic mix, which Section~\ref{sec:transfer} suggests
is optimistic; read them as an upper bound. The serving measurements are
single-host, with the scheduler's contribution named as unmeasured.

\section{Conclusion}
\label{sec:conclusion}
On CICIDS2017, with everything but the split held fixed, most of a benchmark
score belongs to the protocol: attack recall for the tree ensembles falls
from about 0.99 to about 0.31 once no attack family is shared between
training and test, and the ranking inverts, so the conventional protocol can
recommend the wrong model. A single split is not a result; nor is an
aggregate score over families whose detectability differs by an order of
magnitude; nor does benchmark precision survive a production prevalence,
where the degradation is invisible to an operator watching alert volume. The
served endpoint sustains 404~requests/s where its amortised model cost
predicts $9.5\times10^{5}$~flows/s, and one environment variable decides
whether adding replicas helps or hurts. None of this says learned intrusion
detection does not work; it says the published numbers answer an easier
question than the operational one, and the gap between protocols, cheap to
compute, belongs beside the conventional figure. Next: the protocol on a
corrected~\cite{lanvin2023errors} and a contemporary corpus; one-class and
anomaly-based detectors under the same day-disjoint split, where they may
overturn the ordering here; and the cluster-level serving measurements.


% Acknowledgements: add here if funding is to be named.

% Required by ICTA 2026 (placed immediately before References).
\subsubsection*{AI Declaration.}
Generative AI tools (Anthropic Claude) assisted with language editing,
LaTeX preparation and scaffolding of the experiment code. All experiments
were designed and executed by the authors, every reported number is an actual
measured result, and the authors take full responsibility for this paper.

\bibliographystyle{splncs04}
\bibliography{references}

\end{document}
