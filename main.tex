% =====================================================================
%  Network Intrusion Detection on CICIDS2017
%  Springer LNCS / CCIS format  (target venues: ICTA 2026, FDSE 2026)
%  Compiled FREE on GitHub Actions via xu-cheng/latex-action@v3
%
%  GOLDEN RULE: every number in this paper must come from the Colab
%  experiments (experiments/ids_cicids2017.ipynb). Placeholders are
%  marked TODO and must be replaced with real measured values.
% =====================================================================
% SOICT requires the submitted PDF to carry no page numbers, so the
% running heads are dropped for submission.
\documentclass{llncs}
% SOICT 2026 is single-blind: \anonfalse keeps author information visible.
% Set \anontrue only for a double-blind venue.
\newif\ifanon\anonfalse

\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{amsmath}
\usepackage{multirow}
\usepackage{array}
\usepackage{url}
\usepackage{tikz}
\usetikzlibrary{positioning,fit,arrows.meta}
% NOTE: hyperref intentionally NOT loaded — it clashes with llncs
% (\@xdblarg error). Springer adds hyperlinking for the final version.

% Tables are generated by experiments/make_tables.py directly from
% experiments/results_v3/*.json, so no measured number is typed by hand.
% A stage that has not been run yet simply contributes no table.
\newcommand{\inputtable}[1]{%
  \IfFileExists{tables/#1.tex}{\input{tables/#1}}{}}

% Figures are generated by experiments/make_figures.py from the same
% JSON as the tables. A figure whose stage has not run is skipped rather
% than breaking the build.
\newcommand{\resfig}[3]{%
  \IfFileExists{figures/#1.pdf}{%
    \begin{figure}[tb]\centering
    \includegraphics[width=\textwidth]{figures/#1}
    \caption{#2}\label{#3}
    \end{figure}}{}}

\pagestyle{empty}
\begin{document}
\thispagestyle{empty}

\title{Network Intrusion Detection on CICIDS2017: A Comparative Study of
Machine Learning and Deep Learning Models with a Cloud-Native
Deployment Design}

\ifanon
\author{Anonymous Author(s)}
\institute{Affiliation withheld for double-blind review}
\else
\author{Huy-Thanh Pham\inst{1} \and Tuan-Anh Truong\inst{1} \and
Vinh-Khuong Truong\inst{2}}
\authorrunning{H.-T. Pham et al.}
\institute{Faculty of Computer Science and Engineering,\\
Ho Chi Minh City University of Technology (HCMUT), VNU-HCM, Vietnam \\
\email{huythanh@clouds.io.vn, anhtt@hcmut.edu.vn}
\and Ton Duc Thang University, Ho Chi Minh City, Vietnam \\
\email{trvinhkhuong@gmail.com}}
\fi

\maketitle

\begin{abstract}
Machine-learned intrusion detectors report near-perfect scores on public
benchmarks, and practitioners do not see that performance in the field. We
show that a large part of the discrepancy is produced by the evaluation
itself, and we measure how large. Holding the models, the hyperparameters and
the preprocessing fixed on CICIDS2017, and re-fitting feature selection,
oversampling and scaling inside every training split, we compare a
conventional stratified random split against a day-disjoint split in which no
attack family is shared between training and test. Attack recall for XGBoost
falls from 0.9935 to 0.3164 and macro-F1 from 0.9879 to 0.6887; Random Forest
behaves the same way. The ranking of models does not survive either: Logistic
Regression, the weakest detector under the random split, becomes the
strongest under the day-disjoint one, so a benchmark that flatters a model can
also select the wrong one. Repeating the conventional protocol over five
seeds further shows that the usual claim of a best model is unsupported --
XGBoost leads Random Forest by 0.00044 macro-F1, which neither a paired
$t$-test nor a Wilcoxon signed-rank test separates from zero. We then
translate the measured error rates into precision at operational attack
prevalence, where benchmark precision does not transfer, and we report the
measured end-to-end latency distribution of the model served as a
containerised endpoint under controlled concurrency -- deliberately separate
from the amortised per-sample model cost that the literature, and an earlier
version of this work, mistook for a service throughput. The experiment
driver, the serving implementation, the deployment manifests and the raw
result files from which every table and figure here is generated are released
with the paper.
\keywords{Intrusion detection \and Evaluation methodology \and Data leakage
\and CICIDS2017 \and Model serving \and Network security.}
\end{abstract}

% =====================================================================
\section{Introduction}
\label{sec:intro}
Enterprise and telecommunication networks face a growing volume and variety
of attacks, from volumetric denial-of-service campaigns to stealthy
infiltration. Manual analysis does not scale to modern traffic, which is why
learning-based intrusion detection systems (IDS) have become the dominant
approach~\cite{buczak2016survey}. The published accuracy of these systems is
extraordinarily high: on standard benchmarks, macro-F1 above 0.98 is
routine, and a reader could reasonably conclude that network intrusion
detection is close to solved.

Practitioners do not observe this. The gap between a benchmark score and
field behaviour is usually attributed to concept drift or to the age of the
corpora, and both certainly contribute. This paper examines a more mundane
and more immediate contributor: much of the reported accuracy is an artefact
of how the benchmark is split and of what the reported metric measures. Two
specific mechanisms are at work.

\paragraph{A random split rewards recognition, not detection.}
Flows belonging to a single attack episode are numerous, near-identical, and
scattered by a random split across the train/test boundary. A model can score
extremely well by recognising attack instances whose near-duplicates it has
already memorised -- a capability that has little to do with the operational
task of flagging something new. That random splits flatter intrusion
detectors has been observed before~\cite{moczkodan2026transformers,xu2025robust},
and we do not claim the observation. What we contribute is a protocol in
which the confound is removed rather than merely acknowledged, and a
measurement of what it costs.

\paragraph{Benchmark precision does not survive contact with real traffic.}
Precision depends on the prevalence of attacks, and benchmark corpora contain
orders of magnitude more attack traffic than a production link. A precision
of 0.97 measured at a 17\,\% attack rate says almost nothing about the alert
queue an analyst would face at a rate of one in a thousand. Reporting the
former without the latter is not wrong so much as unusable.

A third gap is one of engineering rather than of statistics. Papers that
propose a deployment routinely support it with an \emph{amortised per-sample
model cost} -- the total time to score a test batch, divided by its size, on
all available cores. That figure omits feature extraction, request parsing,
serialisation, transport, queueing and concurrency. It is a lower bound on
the cost of the model, not a property of a service, and converting it into a
throughput is not sound. An earlier version of this work made exactly that
conversion, which is what prompted the present study.

\paragraph{Contributions.}
We hold the models, the hyperparameters and the preprocessing fixed, and vary
only the evaluation. Concretely:

\begin{enumerate}
\item A pipeline in which standardisation, feature selection and
  oversampling are re-fitted \emph{inside} every training split, so that no
  part of the input space is chosen with knowledge of the test data
  (Section~\ref{sec:leakage}).
\item Four splitting protocols evaluated side by side on identical data:
  repeated stratified holdout, day-disjoint (which on CICIDS2017 is
  necessarily also family-disjoint), leave-one-family-out, and genuine
  cross-dataset transfer on an aligned feature space
  (Section~\ref{sec:method}). We report the difference between them, which is
  the quantity a reader can act on.
\item A statistical treatment of model comparison -- repeated seeds, paired
  tests, confidence intervals -- which shows that the ranking commonly
  reported between the two leading models is not supported, while the
  ranking between protocols is large and unambiguous
  (Section~\ref{sec:paired}).
\item A translation of measured error rates into precision and analyst alert
  burden at operational attack prevalence (Section~\ref{sec:operational}).
\item An end-to-end measurement of the deployed service -- the full latency
  distribution under controlled concurrency, and the per-record versus
  batched operating points -- reported separately from, and never derived
  from, model cost (Section~\ref{sec:serving}).
\end{enumerate}

The artefact accompanying this paper contains the experiment driver, the
serving implementation, the container image definition, the Kubernetes
manifests, the load profile, and the raw result files from which every table
in this paper is generated.

\paragraph{Organization.}
Section~\ref{sec:related} positions the work. Section~\ref{sec:data}
describes the data and preprocessing, Section~\ref{sec:method} the evaluation
protocol. Section~\ref{sec:results} reports the results,
Section~\ref{sec:serving} the measured serving cost.
Section~\ref{sec:limitations} states what the study does not establish, and
Section~\ref{sec:conclusion} concludes.

% =====================================================================
\section{Related Work}
\label{sec:related}
Machine learning has been applied to intrusion detection for two decades;
the survey of Buczak and Guven~\cite{buczak2016survey} covers the method
families and the trade-off between detection quality, interpretability and
computational cost. Our concern here is narrower, and it is with evaluation
rather than with models: what a reported score on a public benchmark does,
and does not, tell a practitioner.

\paragraph{Benchmarks and their defects.}
KDD'99 and its refinement NSL-KDD~\cite{tavallaee2009nslkdd} exposed and
partly corrected redundant records and unrealistic class balance.
UNSW-NB15~\cite{moustafa2015unswnb15} introduced a more contemporary feature
set, and CICIDS2017~\cite{sharafaldin2018cicids} added five days of labelled
traffic mixing benign behaviour with several attack families. CICIDS2017 is
not a neutral instrument, however. Engelen et
al.~\cite{engelen2021troubleshooting} document labelling and traffic-capture
defects -- misordered and duplicated packets, and attacks that were executed
but left unlabelled -- and Lanvin et al.~\cite{lanvin2023errors} show that
correcting such errors changes measured detection performance materially.
We therefore treat absolute scores on this corpus as properties of the
benchmark rather than of the detector, and report differences between
protocols on identical data, which are far less sensitive to label noise than
the levels themselves.

\paragraph{Evaluation protocol.}
That random splits flatter intrusion detectors is not a new observation, and
we do not claim it as one. Moczkodan and Ragab~\cite{moczkodan2026transformers}
compare random, temporal and five-tuple-grouped splits on CICIDS2017 and find
that the random-split convention substantially overestimates model
performance. Xu and Liu~\cite{xu2025robust} likewise report that supervised
models reaching near-perfect accuracy on familiar attacks suffer severe
recall loss on novel ones. Our contribution is not the observation but its
extension along three axes those studies leave open: a \emph{day-disjoint}
split, which on this corpus is simultaneously family-disjoint because each
attack family is confined to a single capture day; a translation of the
resulting error rates into precision at operational attack prevalence, which
is orders of magnitude below the benchmark ratio; and a measurement of what
the chosen detector costs to actually serve.

\paragraph{From model cost to service cost.}
The last axis is the one the literature treats most loosely. It is common to
report an amortised per-sample model cost -- figures on the order of
$10^{-2}$~ms per sample are typical -- and to present it as evidence of
deployability. Such a figure is a lower bound on model cost measured over a
full batch on all available cores; it excludes feature extraction, request
parsing, serialisation, transport, queueing and concurrency, and it is not a
property of a running service. We report per-sample model cost too, but
separately from, and never converted into, the end-to-end latency
distribution of a deployed endpoint under controlled load.

Recent applied studies confirm the continued use of this benchmark and the
strength of tabular models on it: Talukder et al.~\cite{talukder2024ml}
combine oversampling with stacking-based feature embedding for large
imbalanced traffic, and Almuhanna and Dardouri~\cite{almuhanna2025dl} contrast
deep and classical models for anomaly-based detection.
Table~\ref{tab:related} summarises how this paper is positioned.

\inputtable{related_work}

% =====================================================================
\section{Data and Preprocessing}
\label{sec:data}
CICIDS2017~\cite{sharafaldin2018cicids} contains five working days of
labelled network flows -- benign traffic together with denial-of-service,
distributed denial-of-service, brute-force, web-attack, botnet, port-scan and
infiltration families -- each flow described by more than eighty statistical
features derived from packet and flow-level measurements. The corpus is
distributed as eight CSV files, one per capture period. For computational
tractability we draw a fixed 20\,\% stratified sample from each file with a
fixed seed.

Two pieces of information that a binary formulation would discard are
retained, because the protocols of Section~\ref{sec:method} depend on them.
First, each flow keeps its \emph{capture day}, recovered from the file it
came from; this is what makes the day-disjoint split possible. Second, each
flow keeps its \emph{original multi-class label} alongside the binary one, so
that per-family recall and leave-one-family-out can be computed. The binary
target is formed by mapping every non-benign label to the positive class.

Cleaning normalises column names, replaces infinite values with missing
markers, drops records with missing values, and removes exact duplicates.
Table~\ref{tab:dataset} gives the resulting composition and
Table~\ref{tab:days} the distribution over capture days. The class imbalance
is pronounced -- and, as Section~\ref{sec:operational} argues, still far
milder than an operational link.

Standardisation, top-$k$ feature selection by Random Forest
importance~\cite{breiman2001random}, and oversampling with
SMOTE~\cite{chawla2002smote} are all part of the model rather than of the
data: each is fitted on the training portion of whichever split is in force,
and never on the corpus as a whole. Section~\ref{sec:leakage} states this
precisely, since it is one of the two things the paper varies.

% =====================================================================
\section{Evaluation Protocol}
\label{sec:method}
The models in this study are deliberately ordinary. What varies, and what the
paper is about, is the protocol under which they are evaluated. This section
fixes that protocol precisely enough to be re-run.

\subsection{Models and Hyperparameters}
We compare four detectors spanning a representative range of capacity and
cost. \emph{Logistic Regression} is a linear baseline. \emph{Random
Forest}~\cite{breiman2001random} is a bagging ensemble of decision trees.
\emph{XGBoost}~\cite{chen2016xgboost} is a gradient-boosted tree method that
is consistently competitive on tabular data. A \emph{multilayer perceptron}
(MLP) with two hidden layers represents the neural family.

Table~\ref{tab:hyper} lists every hyperparameter. The values are fixed a
priori and are identical across every split, seed and dataset in this paper.
We perform no tuning at all. This is a deliberate choice with a cost and a
benefit: the absolute scores are certainly not the best attainable, but no
model is advantaged by a larger search budget than another, and no
hyperparameter can have been selected -- however indirectly -- on the test
data. Because the paper reports differences between evaluation protocols on
a fixed pipeline, rather than a claim of state-of-the-art accuracy, the
absence of tuning removes a confound instead of weakening the result. We
record it as a limitation in Section~\ref{sec:limitations} nonetheless.

\inputtable{hyperparameters}

\subsection{Leakage Control Inside the Pipeline}
\label{sec:leakage}
Preprocessing that is fitted before the data is split leaks test information
into training even when the classifier never sees a test row. Feature
selection is the worst offender: choosing the top-$k$ features by importance
computed over the whole corpus lets the test set influence the input space of
every subsequent model.

We therefore treat standardisation, feature selection and oversampling as
part of the model rather than as preparation of the data. For every split, in
every protocol, the scaler is fitted on the training portion only; the
Random-Forest importance ranking that selects the top $k=20$ features is
computed on the scaled training portion only; and
SMOTE~\cite{chawla2002smote} is applied to the training portion only, after
selection. The test portion is transformed by the already-fitted scaler and
reduced to the already-chosen features. Nothing is fitted twice and nothing
is fitted on the union. Since the ranking is recomputed per split, the
selected feature set is itself a random variable, and we report its stability
in Section~\ref{sec:featstab} rather than presenting one list as canonical.

\subsection{Four Splitting Protocols}
The same pipeline is evaluated under four separations of training from test,
in increasing order of difficulty. Reporting them side by side is the point:
each protocol answers a different question, and only the last two resemble
what a deployed sensor faces.

\paragraph{(P1) Repeated stratified holdout.}
A stratified 70/30 split, repeated over five seeds. This is the conventional
protocol and serves as the reference point. Repetition is not cosmetic: a
single split gives no way to distinguish a real difference between models
from split-to-split noise, and Section~\ref{sec:stats} shows that some
differences on this corpus are smaller than that noise.

\paragraph{(P2) Day-disjoint.}
Training uses flows captured on Monday to Wednesday; testing uses Thursday
and Friday. No flow from a capture day appears on both sides, which removes
the near-duplicate flows that a random split scatters across the boundary.
On CICIDS2017 this split has a second and stronger consequence: because each
attack family was executed on one specific day, a day-disjoint split is
necessarily also \emph{family-disjoint}. Every attack in the P2 test set is
of a type entirely absent from training, so P2 measures detection of unseen
attack types, not recognition of familiar ones.

\paragraph{(P3) Leave-one-family-out.}
P2 confounds the day boundary with the family boundary. P3 separates them:
one attack family is removed from training entirely and the model is then
asked to flag it. Repeating this for each family gives a per-family measure
of generalisation to novel attacks, and shows that the aggregate binary score
is an average over families whose individual detectability differs by a wide
margin.

\paragraph{(P4) Cross-dataset transfer.}
The strictest test changes the corpus. Since CICIDS2017 and UNSW-NB15 do not
share a feature schema, transfer requires an explicit alignment; we use the
seven flow-level quantities that both provide with compatible semantics,
rescaling flow duration from microseconds to seconds
(Table~\ref{tab:align}). A model is fitted on one corpus and evaluated on the
other with no target-domain data of any kind. We distinguish this from the
weaker \emph{independent-dataset replication}, in which the whole protocol is
re-run within UNSW-NB15 to check whether the ordering of models reproduces on
independent data. The two are frequently conflated, including in an earlier
version of this work; only P4 says anything about cross-domain behaviour.

\subsection{Statistical Treatment}
\label{sec:stats}
Every protocol that admits repetition is repeated over multiple seeds, and
all four models are evaluated on the identical splits, so comparisons are
paired. We report the mean and the half-width of the 95\,\% confidence
interval of the mean rather than a single figure. For the two leading models
we additionally report a paired $t$-test and a Wilcoxon signed-rank test over
the shared seeds, so that a claim that one model beats another is backed by a
test rather than by a difference in the fourth decimal place.

\subsection{Metrics}
Let $TP$, $TN$, $FP$ and $FN$ denote true positives, true negatives, false
positives and false negatives. Alongside accuracy, precision, recall and
macro-averaged F1
\begin{equation}
\mathrm{Precision}=\frac{TP}{TP+FP},\qquad
\mathrm{Recall}=\frac{TP}{TP+FN},\qquad
\mathrm{F1}=\frac{2\,\mathrm{PR}}{\mathrm{P}+\mathrm{R}},
\end{equation}
we report the false-positive and false-negative rates, the Matthews
correlation coefficient, balanced accuracy, and the area under both the ROC
and the precision--recall curve. PR-AUC is the more informative of the two
under heavy class imbalance, because ROC-AUC is insensitive to the absolute
number of false positives when negatives vastly outnumber positives.

\paragraph{Operational prevalence.}
Benchmark corpora contain a far higher share of attacks than any production
link. Since precision depends on prevalence while TPR and FPR do not, a
precision measured at benchmark prevalence does not transfer to deployment.
For an attack prevalence $\pi$, the precision implied by a measured
$(\mathrm{TPR}, \mathrm{FPR})$ pair is
\begin{equation}
\label{eq:ppv}
\mathrm{PPV}(\pi)=\frac{\pi\cdot\mathrm{TPR}}
{\pi\cdot\mathrm{TPR}+(1-\pi)\cdot\mathrm{FPR}} ,
\end{equation}
and we evaluate it at $\pi \in \{10^{-1}, 10^{-2}, 10^{-3}, 10^{-4}\}$,
reporting alongside it the number of false alerts per million flows, which is
the quantity that determines whether an analyst can use the detector at all.

\subsection{Cost Measurement}
Training time and inference time are measured on the environment of
Table~\ref{tab:env}. Inference is timed after a discarded warm-up pass, as
the mean of five repeats, on the full test batch with all cores available.
The resulting per-sample figure is an amortised lower bound on model cost.
We do not convert it into a service throughput. What a service actually
sustains is measured separately, on the running endpoint, in
Section~\ref{sec:serving}.

% =====================================================================
\section{Results}
\label{sec:results}
We report the four protocols of Section~\ref{sec:method} in order, then the
feature-selection analysis and the operational reading of the error rates.
Every number below is generated directly from the stored experiment output;
none is transcribed by hand.

\inputtable{dataset}
\inputtable{days}

\subsection{P1: Repeated Stratified Holdout}
\label{sec:p1}
Table~\ref{tab:holdout} gives the conventional protocol, repeated over five
seeds. The two tree ensembles are close to each other and clearly ahead of
the linear and neural baselines. The interval half-widths are worth as much
attention as the means: they are tight for Random Forest and XGBoost and wide
for Logistic Regression and the MLP, so a single-split result for either of
the latter two is close to uninformative.

\inputtable{holdout}
\resfig{fig_seed_spread}{Macro-F1 of every model on every seed under the random split, with the mean and its 95\,\% confidence interval. The two tree ensembles are so stable that their intervals are hard to see at this scale, while Logistic Regression and the MLP vary enough between seeds that a single-split number for either would be misleading.}{fig:seedspread}

\subsection{Is the Leading Model Actually Leading?}
\label{sec:paired}
The usual conclusion drawn from a table like the one above is that XGBoost
wins. Table~\ref{tab:paired} tests that claim on the paired seeds rather than
asserting it. XGBoost does lead Random Forest, but by a margin in the fourth
decimal place, and neither the paired $t$-test nor the Wilcoxon signed-rank
test rejects equality at any conventional level. Both ensembles are, however,
separated from Logistic Regression and from the MLP by margins that are large
and clearly significant.

We therefore do not claim a best model. What the data supports is a
separation into two tiers -- tree ensembles above linear and shallow neural
baselines -- and within the upper tier the choice may be made on other
grounds. Section~\ref{sec:cost} shows those grounds are not negligible.

\inputtable{paired_tests}

\subsection{P2: Day-Disjoint, and the Optimism of a Random Split}
\label{sec:p2}
The day-disjoint split trains on Monday--Wednesday and tests on
Thursday--Friday. On this corpus the two sides share no attack family at all:
training contains the denial-of-service family, the two brute-force families
and Heartbleed, while the test side contains botnet, DDoS, infiltration, port
scanning and the three web-attack variants. Every attack the detector meets
at test time is therefore of a type it has never seen.

\inputtable{day_disjoint}
\resfig{fig_confusion}{Row-normalised confusion for XGBoost, averaged over seeds, with raw counts in parentheses. The false-positive rate improves slightly under the day-disjoint protocol while the false-negative rate rises by two orders of magnitude: the detector does not become noisier, it becomes blind.}{fig:confusion}
\inputtable{optimism_gap}
\resfig{fig_optimism_gap}{The same pipeline, the same hyperparameters and the same data, evaluated two ways. Filled markers are the random split, hollow markers the day-disjoint split; the bar between them is the optimism of the conventional protocol. The ordering of the models is not preserved: the weakest detector under the random split is the strongest once the attack families are held out.}{fig:gap}

\subsection{P3: Per-Family Behaviour and Unseen Attack Types}
\label{sec:p3}
\inputtable{per_family}
\resfig{fig_per_family}{Recall by attack family under the random split. The aggregate score is an average over families whose individual detectability differs by an order of magnitude.}{fig:family}
\inputtable{leave_one_out}
\resfig{fig_leave_one_out}{Leave-one-family-out: recall on a family removed from training altogether.}{fig:loo}

\subsection{Feature Selection: Reproducible but Not Canonical}
\label{sec:featstab}
\inputtable{feature_stability}
\resfig{fig_feature_stability}{The selected feature set is highly reproducible across seeds and much less so across importance criteria, so it should be read as one defensible choice rather than as the informative subset.}{fig:featstab}
\inputtable{feature_list}

\subsection{Independent Replication, and P4: Cross-Dataset Transfer}
\label{sec:transfer}
\inputtable{unsw}
\inputtable{alignment}
\inputtable{transfer}
\resfig{fig_transfer}{Cross-dataset transfer on the aligned feature space, in both directions.}{fig:transfer}

\subsection{What the Error Rates Mean in Operation}
\label{sec:operational}
\inputtable{operational}
\resfig{fig_ppv_prevalence}{Precision as a function of attack prevalence, computed from the measured true- and false-positive rates. Benchmark corpora sit at the left of this axis; production links sit at the right.}{fig:ppv}

\subsection{Computational Cost}
\label{sec:cost}
\inputtable{cost}
\resfig{fig_cost}{Detection quality against amortised model cost, with 95\,\% intervals on macro-F1. Note the logarithmic cost axis: the two ensembles are indistinguishable in quality and differ materially in price.}{fig:cost}

% =====================================================================
\section{Deployment and Measured Serving Cost}
\label{sec:serving}
An offline score says what a detector would decide; it does not say what the
system around it costs to run. This section closes that gap by measuring the
deployed service rather than describing it.

\subsection{The Service}
The selected model is exported together with the per-column standardisation
statistics of the features it uses, and wrapped in a stateless HTTP service
implemented with FastAPI. The service exposes a liveness endpoint, a
readiness endpoint that reports failure until the model is resident in
memory, a metadata endpoint, and two scoring endpoints: one that scores a
single flow and one that scores a batch. Every response carries a header with
the in-process scoring time, which lets the client separate model cost from
queueing, parsing and transport. The image is pinned, runs as a non-root
user, and runs a single worker per replica so that pod-level scaling is not
confounded by in-process parallelism. Figure~\ref{fig:arch} summarises the
architecture.

\begin{figure}[t]
\centering
\begin{tikzpicture}[
  >={Stealth[length=2mm]},
  box/.style={draw, rounded corners, align=center, font=\small,
              minimum height=9mm, inner sep=5pt},
  node distance=10mm and 12mm]
  \node[box] (flows) {Network flows\\(CICIDS2017)};
  \node[box, right=of flows] (pre) {Preprocessing\\and features};
  \node[box, right=of pre] (svc) {IDS microservice\\(FastAPI, Docker)};
  \node[box, right=of svc] (out) {Alerts /\\dashboard};
  \node[box, above=10mm of svc] (cicd) {CI/CD pipeline\\(build--test--deploy)};
  \draw[->] (flows) -- (pre);
  \draw[->] (pre) -- (svc);
  \draw[->] (svc) -- (out);
  \draw[->, dashed] (cicd) -- (svc);
  \node[draw, dotted, rounded corners, inner sep=9pt,
        fit=(pre)(svc)(out),
        label={[font=\small\itshape]below:Kubernetes cluster}] {};
\end{tikzpicture}
\caption{Cloud-native serving architecture: the selected model is exposed as a
FastAPI microservice, containerized with Docker, deployed on a Kubernetes
cluster, and released through an automated CI/CD pipeline.}
\label{fig:arch}
\end{figure}

The artefact served is the artefact evaluated: it is fitted by the same
pipeline, on the same training split, with the leakage controls of
Section~\ref{sec:leakage}. The latency reported below therefore belongs to a
model whose detection quality is reported in Section~\ref{sec:results}, and
not to a placeholder.

\subsection{Load Protocol}
Load is generated in closed loop: each of $N$ clients issues its next request
as soon as the previous response returns, which is the behaviour of a bounded
worker pool and, unlike an open-loop rate test, cannot mask saturation behind
an unbounded queue. Each concurrency level begins with a warm-up phase whose
samples are discarded. The payloads are held-out CICIDS2017 flows replayed in
sequence, so the service parses realistic values rather than synthetic
vectors. We report the full latency distribution -- median through the
99.9th percentile -- because a mean latency conceals exactly the tail that
determines whether an inline sensor is usable.

\subsection{Results}
\label{sec:servingresults}
\inputtable{serving_latency}
\resfig{fig_serving}{Measured latency distribution and sustained throughput of the deployed service against the number of concurrent clients. Two panels share one x axis rather than overloading a single plot with two scales.}{fig:serving}
\inputtable{serving_batch}
\resfig{fig_serving_batch}{The batching trade-off: flows per second and the 95th-percentile request latency as the batch grows.}{fig:servingbatch}

\subsection{What Is Not Measured}
The numbers above characterise a single replica under controlled load on the
environment of Table~\ref{tab:env}. Autoscaling response, cold start under
scheduling pressure, and request loss during pod replacement are properties
of a cluster, not of a process, and we do not report them. The deployment
manifests -- including resource requests and limits, both probes, and a
horizontal autoscaler with explicit scale-up and scale-down windows -- and
the load profile are released with the artefact so that these measurements
can be made on a cluster. We state their absence rather than estimating
them; substituting an extrapolation for a measurement here is the specific
error this paper argues against.

\section{Limitations}
\label{sec:limitations}
The result of this paper is a set of measured differences between evaluation
protocols, and it is worth being explicit about how far those differences
carry.

\paragraph{The benchmark is an instrument, not the world.}
CICIDS2017 was captured in 2017 and is known to contain labelling and capture
defects~\cite{engelen2021troubleshooting,lanvin2023errors}; correcting them
changes measured performance~\cite{lanvin2023errors}. Absolute scores on this
corpus should therefore not be read as estimates of field accuracy, and we do
not present them as such. The quantities we emphasise are differences between
protocols evaluated on identical data with an identical pipeline, which are
considerably more robust to label noise than the levels are, since a defect
that inflates a score under one protocol inflates it under the other. What
the paper does not establish is the magnitude of the optimism gap on a
corrected or a modern corpus; that requires re-running the protocol on one,
which the released artefact makes straightforward.

\paragraph{Day-disjoint is not the only leakage-resistant split.}
Grouping by five-tuple or splitting strictly by
timestamp~\cite{moczkodan2026transformers} isolate different sources of
optimism, and a full factorial over splitting strategies would separate the
contribution of near-duplicate flows from that of unseen attack families more
cleanly than the day boundary can. We chose the day boundary because on this
corpus it coincides with the family boundary and therefore answers the
operationally important question -- what happens on an attack type never
seen before -- with a single, simply-stated protocol.

\paragraph{No hyperparameter tuning.}
Section~\ref{sec:method} fixes all hyperparameters a priori. This keeps the
comparison between protocols clean, but it means the absolute scores are not
the best each model family can reach, and it leaves open whether a tuned
model would close, or widen, the gap between protocols. We would expect
tuning against a random-split validation set to widen it.

\paragraph{Prevalence is modelled, not observed.}
The operational precision figures follow from Equation~\ref{eq:ppv} applied
to measured TPR and FPR. They are exact given those rates and the assumed
prevalence, but they assume the rates themselves transfer to a link with a
different traffic mix, which the transfer experiment suggests is optimistic.
They should be read as an upper bound on what the detector would deliver at
that prevalence, not as a prediction.

\paragraph{The serving measurement is single-node.}
Section~\ref{sec:serving} reports the latency distribution of the running
service under controlled load on the environment of Table~\ref{tab:env}.
Cluster-level behaviour -- autoscaling response, cold start under scheduling
pressure, and recovery after pod loss -- requires a Kubernetes deployment and
is not measured here. We release the deployment manifests and the load
profile so the missing measurements can be produced, and we mark them as
absent rather than estimating them: extrapolating deployment behaviour from
model cost is precisely the error this paper argues against.

% =====================================================================
\section{Conclusion}
\label{sec:conclusion}
We set out to measure how much of a benchmark intrusion-detection score is a
property of the detector and how much is a property of the protocol. On
CICIDS2017, with everything but the split held fixed, the answer is that most
of the headline number is protocol. Attack recall for the two tree ensembles
falls from about 0.99 to about 0.31 when no attack family is shared between
training and test, and the ranking of the models inverts, so the conventional
protocol does not merely inflate a score -- it can also recommend the wrong
model.

Three consequences follow for how such systems should be reported. First, a
single split is not a result: over five seeds the difference between the two
leading models is smaller than the seed-to-seed variation of the weaker two,
and a paired test does not separate it from zero. Second, an aggregate binary
score is not a result either, because it averages over families whose
individual detectability differs by an order of magnitude, and the families
that vanish into the average are not the harmless ones. Third, precision
measured at benchmark prevalence does not survive the move to a production
link, where the same error rates imply a very different alert queue.

The engineering claim is subject to the same discipline. We report the
latency distribution of the running service under controlled concurrency, and
we do not derive it from the amortised per-sample cost of the model; the
cluster-level properties we did not measure are named as unmeasured rather
than estimated. An earlier version of this work made exactly the
extrapolation we now argue against, which is part of why we argue against it.

None of this says that learned intrusion detection does not work. It says
that the published numbers answer a question -- can the model recognise more
instances of attacks it has already seen? -- that is easier than the
operational one. The gap between the two protocols is a usable estimate of
that difference, it is cheap to compute, and we would encourage its reporting
alongside the conventional figure rather than in place of it.

Three directions follow directly. The protocol should be re-run on a
corrected~\cite{lanvin2023errors} and on a contemporary corpus, to separate
the effect of the split from the defects of this particular dataset.
Detectors designed for unseen families -- one-class and anomaly-based
formulations -- should be evaluated under the same day-disjoint protocol,
where they may well overturn the ordering reported here. And the
cluster-level serving measurements left open in
Section~\ref{sec:limitations} should be completed, since autoscaling
behaviour under attack-driven load spikes is precisely the case an intrusion
detector must survive.

\ifanon\else
\subsubsection*{Acknowledgements.} % TODO (tuỳ chọn): lời cảm ơn / nguồn tài trợ.
\fi

% Required by ICTA 2026 (placed immediately before References).
\subsubsection*{AI Declaration.}
Generative AI tools (Anthropic Claude) were used to assist with language
editing, LaTeX preparation, and scaffolding of the experiment code. All
experiments were designed and executed by the authors, every reported number
is an actual measured result, and the authors have reviewed and verified all
content and take full responsibility for this paper.

\bibliographystyle{splncs04}
\bibliography{references}

\end{document}
